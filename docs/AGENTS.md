# Agents

- Pipeline agent: orchestrates all 4 phases.
- Apify agent: executes configured actors.
- Minds scoring agent: returns VC viability + top 10 VCs.
- OpenAI narrative agent: writes one-pager.
- Storyboard agent: builds multi-panel pitch (OpenAI copy + Pollinations images; template fallback).
